import { UserProfile } from "@clerk/react";

const Settings = () => {
  return (
    <div className="flex h-screen overflow-y-auto justify-center items-center">
      <UserProfile />
    </div>
  )
}

export default Settings
